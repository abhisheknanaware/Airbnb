<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Airnb</title>
    <link rel="stylesheet" href="/hosthome.css">
    <link rel="stylesheet" href="/nav.css">
</head>

<body>
    <%-include('../partials/nav')%>
        <h2 class="head2">Hey Host! Here are yours homes:</h1>


            <div class="container">
                <div class="card-grid">
                    <% if (Array.isArray(arr) && arr.length > 0) { %>
                        <% arr.forEach(function(homename, index) { %>
                            <div class="card" role="article">

                                <div class="card-image-wrapper">
                                    <img src="<%- (homename && homename.image_url) ? ((typeof homename.image_url === 'string' && homename.image_url.indexOf('data:') === 0) ? homename.image_url : (homename.image_url.length > 1000 ? 'https://via.placeholder.com/800x500?text=Image+Too+Large' : homename.image_url)) : 'https://via.placeholder.com/800x500?text=No+Image' %>"
                                        alt="<%= (homename && homename.housename) ? homename.housename : 'Property' %>" 
                                        class="card-image" 
                                        loading="lazy" />
                                    <div class="price-badge">$<%= (homename && homename.price) != null ? homename.price : '—' %></div>
                                </div>

                                <div class="card-content">
                                    <h3 class="card-title">
                                        <%= (homename && homename.housename) ? homename.housename : 'Unnamed Home' %>
                                    </h3>

                                    <p class="card-location">📍 <%= (homename && homename.location) ? homename.location : 'Unknown location' %></p>

                                    <div class="card-details">
                                        <div class="rating" title="Rating">
                                            <span class="star">★</span>
                                            <span class="rating-value">
                                                <%= (homename && typeof homename.rating === 'number') ? homename.rating.toFixed(1) : ((homename && homename.rating) ? homename.rating : 'N/A') %>
                                            </span>
                                        </div>

                                        <div class="card-actions" aria-hidden="false">
                                            <a href="/homes/<%= homename._id %>" class="btn-view"
                                                aria-label="View <%= (homename && homename.housename) ? homename.housename : 'Unnamed Home' %>">View</a>

                                            <a href="/host/edit-home/<%= homename._id%>?editing=true" class="btn-edit" 
                                               aria-label="Edit <%= (homename && homename.housename) ? homename.housename : 'Unnamed Home' %>">Edit</a>
                                            
                                            <form action="/host/delete/<%= homename._id%>" method="POST">
                                                <button type="submit" class="btn-delete">Delete</button>
                                            </form>
                                            
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
            </div>

<style>

</style>

</body>

</html>